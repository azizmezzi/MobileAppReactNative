module.exports = {
  preset: 'react-native',
  // setupFiles: ['<rootDir>/jest/setup.js'],
  collectCoverage: true,
  setupFilesAfterEnv: ['<rootDir>/setup-tests.js'],
  transformIgnorePatterns: ['<rootDir>/node_modules/(?!native-base)/'],
  testEnvironment: 'jsdom',
};
